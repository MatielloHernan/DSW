.transaction-item {
  position: relative; 
  padding: 8px;
  margin: 4px 0;
  background-color: #f0f0f0;
  border-radius: 4px;
  transition: background-color 0.3s;
}

.transaction-item:hover {
  background-color: #e0e0e0;
}

.remove-button {
  float: right;
  margin-left: 10px; 
  cursor: pointer;
  background-color: red; 
  color: white;
  border: none; 
  padding: 5px 10px; 
  border-radius: 4px; 
  display: inline-block; 
}

.remove-button:hover {
  background-color: darkred; 
}

.positive {
  background-color: #d4edda; 
}

.negative {
  background-color: #f8d7da;
  color: #d1a100;
}

/* Estilos para os botões de mover */
.move-button {
  background-color: #007bff; 
  color: white; 
  border: none;
  margin-left: 5px; 
  padding: 5px; 
  cursor: pointer;
  border-radius: 4px;
}

.move-button:hover {
  background-color: #0056b3;
}

/* Estilos para o saldo da transação */
.transaction-balance {
  display: block;
  font-size: 0.9em;
  color: #555;
  margin-top: 5px;
}
